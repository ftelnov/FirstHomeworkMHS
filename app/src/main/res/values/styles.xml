<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Header">
        <item name="android:textSize">@dimen/headerSize</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="cardHeader">
        <item name="android:textSize">@dimen/cardHeaderSize</item>
        <item name="android:textColor">@android:color/black</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="cardSubHeader">
        <item name="android:textSize">@dimen/cardSubHeaderSize</item>
    </style>

    <style name="buttonText">
        <item name="android:textSize">@dimen/buttonTextSize</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="skillName">
        <item name="android:textSize">@dimen/skillNameSize</item>
        <item name="android:textColor">@android:color/black</item>
    </style>
    <style name="skillExp">
        <item name="android:textSize">@dimen/skillExpSize</item>
    </style>
    <style name="checkBoxText">
        <item name="android:textSize">@dimen/checkBoxTextSize</item>
    </style>
</resources>